These files are the induvidual test modules for the different exercises.
They are tests to see if the uart, adc, sram etc works. To run them you
need to replace the main.c file with one of these tests.