#include "../inc/can.h"

void can_init()
{
    // -Initialize loop back mode
}

void can_send(can_message *message)
{
    // -Send a message with ID and Data
}

char can_receive()
{
    // -Receive a message
}
